Read and follow `subskills/ui-link/SKILL.md` completely.

Run the executable link workflow:

`python scripts/ui_link_workflow.py --root .`

The wrapper scans prototypes, writes `.ui-bridge/task-html-map.json`, updates safe Spec Kit
references, and writes `.ui-bridge/link-report.md`.
