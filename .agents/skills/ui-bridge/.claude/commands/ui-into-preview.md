Read and follow `subskills/ui-preview/SKILL.md` completely.

Run the executable preview workflow from the project root:

```bash
python scripts/ui_preview_workflow.py --root .
```

The wrapper auto-resolves the prototype folder, the active Spec Kit feature directory, the page map, and writes `ui-preview/` for local review or GitHub Pages deployment.
