---
description: "Approve a UI Bridge brand proposal generated by proposal mode."
---
# ui-approve-brand

Run:

```bash
python scripts/approve_brand.py --root . --proposal A
```

Replace `A` with the user-selected proposal. This writes `.ui-bridge/approvals.json`.
